<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Permitir HTTP (cleartext) a cualquier host en debug para que la URL que configuras en la app funcione sin tocar este archivo. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>


